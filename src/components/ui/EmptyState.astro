---
const {
  lang,
  serverT,
  titleKey,
  descKey,
  actionHref,
  actionKey,
  id,
} = Astro.props as {
  lang: string
  serverT: (k: string) => string
  titleKey: string
  descKey?: string
  actionHref?: string
  actionKey?: string
  id?: string
}
---

<section id={id} role="region" aria-live="polite" data-testid="empty-state" class="card stack text-center">
  <h3 class="h3 no-margin">{serverT(titleKey)}</h3>
  {descKey && <p class="p no-margin text-muted">{serverT(descKey)}</p>}
  {actionHref && actionKey && (
    <a href={actionHref} class="btn btn-primary">{serverT(actionKey)}</a>
  )}
</section>


